# 验收记录

## 已完成

- 当前目录已初始化为 Git 仓库。
- 已固定上游 Spring Boot 版本为 `v4.1.0`。
- 已实现文档源同步脚本。
- 已实现 DeepSeek `deepseek-v4-flash` 翻译脚本。
- 已实现 AsciiDoc 校验脚本。
- 已实现 Antora 中文站构建配置。
- 已生成全量中文页面和 partial，顶层导航已切换到 `nav.adoc`。
- 已补齐 Maven Plugin、Gradle Plugin 和 Actuator REST API 的 Antora 页面。
- 已新增 `npm run sync:generated-content`，可从完整 Spring Boot checkout 执行官方 Gradle 聚合任务并导入生成型 Antora 内容。
- 已导入并翻译官方构建生成的配置属性表、Maven Plugin goal 表、Actuator REST API snippets、自动配置类页面和相关 example 片段，不再生成中文占位 partial。
- 已禁止生成型内容占位残留；`translate:all`、`validate`、完整性审计和构建产物扫描都会把残留占位视为失败条件。
- 已生成无版本前缀兼容入口，兼容当前验收路径。
- 已记录 DeepSeek 成功请求 token 用量到 `reports/deepseek-usage.jsonl`。
- 已同步官方 Java/Kotlin 示例源码，并将 `include-code::` 宏展开为本地 AsciiDoc 代码块。
- 已新增完整性审计，用于发现正文、代码块、链接目标和构建产物中的高置信度缺失问题。
- 已将完整性审计接入 `npm run validate`，并将 HTML 产物扫描接入 `npm run build`。
- 已新增顶层导航重复入口校验，避免 ROOT partial 和模块 partial 重复声明同一个顶层页面。
- 已将正文中的 `javadoc:` 内联宏渲染为可点击官方 API 链接，并禁止构建产物残留原始 `javadoc:` 文本或未解析 `{url-...}` 属性。
- 已新增 Docker 多阶段镜像构建配置，并通过 nginx 容器运行中文静态站点。
- 已新增 GitHub Pages 手动发布文档和 workflow；workflow 只通过 `workflow_dispatch` 触发，会同步固定上游源用于完整性审计，但不执行 DeepSeek 翻译，不读取 `DEEPSEEK_API_KEY`。
- 已将中文内容迁移到多版本目录 `versions/4.1.0/content/boot`，并通过 `site-versions.json` 记录 latest 版本和全部 GA 版本。
- 已支持 `SITE_URL` 覆盖构建地址，GitHub Pages 发布时不会生成 `localhost` canonical 或顶部品牌链接。
- 已在构建产物中写入 `.nojekyll`，并移除默认 UI 回退生成的本机 `Edit this Page` 链接，避免公开页面出现本地 `file://` 源码路径。
- 本站定位为 Spring Boot 中文站，当前提供 Spring Boot `4.1.0` 最新稳定版中文化内容；后续新的稳定版发布后会新增对应中文版本，并保留历史 GA 版本。
- 构建产物会移除 Antora 默认 UI 的顶部黑色导航栏，保留左侧目录、面包屑、右侧 Contents 和版本选择器。
- README 已加入线上站点效果截图和在线访问入口，便于 GitHub 仓库首页直接展示中文站效果。
- README 已按 Spring Boot 官方支持页面 `https://spring.io/projects/spring-boot#support` 记录 2026-06-11 默认展示的主要 GA 分支支持周期，并移除冗长的“当前范围”清单。

## 验收命令

```bash
npm test
npm run validate
npm run audit:completeness
npm run build
SITE_URL=https://example.github.io/spring-boot-zh npm run build
```

当前验收结果：

- `npm test`：通过，110 个测试。
- `npm run validate`：通过，未发现 AsciiDoc 结构、xref、术语、完整性或密钥问题。
- `node --test test/readme.test.mjs`：通过，确认 README 包含站点截图、Pages 入口、Spring Boot `4.1.0`、官方支持页面链接、`4.1.x` / `4.0.x` / `3.5.x` 支持周期，并确认不再包含 `## 当前范围`。
- `npm run audit:completeness`：通过，发现 0 个错误、1177 个警告，并生成本地完整性报告。
- `npm run build`：通过，静态站生成到 `build/site`；Antora 仍会输出既有缺失属性 warning，但未发现 error 级别日志、表格结构错误或 xref 缺失错误。
- `rg '"level":"error"|target of xref not found|table missing leading separator|dropping cells from incomplete row' /tmp/springboot-4.1.0-final-build.log`：无结果。
- `rg '当前中文站暂未纳入完整生成内容|该片段由官方构建流程生成|该表格片段由官方构建流程生成|该请求或响应示例由官方构建流程生成' versions build/site`：无结果。
- `rg '@@(?:ADOC_TOKEN|ADOC_MACRO|CODE_BLOCK|TERM)_\d+@@' versions/4.1.0/content/boot/modules build/site`：无结果。
- `rg 'javadoc:|\{url-[^}]+\}' build/site/boot/4.1.0 -g '*.html'`：无结果。
- `rg 'Edit this Page|<div class="edit-this-page"><a href="file:///' build/site/boot/4.1.0 -g '*.html'`：无结果。
- `rg '<header class="header"|id="topbar-nav"' build/site -g '*.html'`：无结果。
- `rg 'spring-boot-zh-headerless' build/site/boot/4.1.0/index.html build/site/boot/4.1.0/tutorial/first-application/index.html build/site/boot/4.1.0/upgrading.html`：三个页面均包含清除顶部留白和 sticky 偏移的本地样式。
- `docker build -t spring-boot-zh-docs:4.1.0 .`：通过，镜像 `spring-boot-zh-docs:4.1.0` 已生成。
- `docker run --rm -d --name spring-boot-zh-docs -p 8080:80 spring-boot-zh-docs:4.1.0`：通过，容器 `spring-boot-zh-docs` 已启动。
- `curl -fsSI http://127.0.0.1:8080/`：返回 `HTTP/1.1 200 OK`。
- `curl -fsSI http://127.0.0.1:8080/boot/4.1.0/reference/using/structuring-your-code.html`：返回 `HTTP/1.1 200 OK`。
- `npm run serve`：通过，`http-server` 在 `http://127.0.0.1:8080` 暴露 `build/site`；`curl -fsSI http://127.0.0.1:8080/boot/4.1.0/tutorial/first-application/index.html` 返回 `HTTP/1.1 200 OK`，HTML 中保留左侧导航和正文标题，不再包含默认顶部导航。
- GitHub Pages workflow 静态检查：只有 `workflow_dispatch`，没有 `push` 自动发布；包含 `npm run sync:source` 用于完整性审计；不包含 `translate:mvp`、`translate:all` 或 `DEEPSEEK_API_KEY`；发布目录为 `build/site`。
- 多版本产物：`build/site/boot/4.1.0/index.html` 已生成，版本选择器显示 `4.1.0`。
- GitHub Pages 兼容：`build/site/.nojekyll` 已生成，`build/site` 作为 workflow 上传目录。
- GitHub Pages 访问入口：`https://loreshare.github.io/spring-boot-zh-docs/`。
- 当前最新稳定版入口：`https://loreshare.github.io/spring-boot-zh-docs/boot/4.1.0/`。
- 默认顶部导航验收：`rg '<header class="header"|id="topbar-nav"' build/site -g '*.html'` 应无结果。
- 兼容入口：`build/site/maven-plugin/index.html`、`build/site/gradle-plugin/index.html`、`build/site/api/rest/actuator/index.html` 均已生成。
- 示例代码验收：`build/site/boot/4.1.0/how-to/native-image/developing-your-first-application.html` 已渲染 `MyApplication` Java 代码块。
- 导航验收：`build/site/boot/4.1.0/index.html` 中 `tutorial/index.html` 顶层导航链接数量为 1。
- Javadoc 内联链接验收：浏览器打开 `http://127.0.0.1:18080/boot/4.1.0/reference/using/structuring-your-code.html`，目标段落显示 `@ComponentScan`、`@ConfigurationPropertiesScan`、`@EntityScan`、`@SpringBootApplication` 链接，页面正文不再包含原始 `javadoc:`。
- Docker 访问地址：`http://127.0.0.1:8080`。停止容器命令：`docker stop spring-boot-zh-docs`。

GitHub 线上验收：

- 已推送到 `main`。
- 已手动触发 Pages workflow：`https://github.com/LoreShare/spring-boot-zh-docs/actions/runs/27357411165`。
- workflow 通过：测试、同步上游源、校验、构建、上传 Pages 产物和部署 Pages 均成功。
- `curl -fsSI https://loreshare.github.io/spring-boot-zh-docs/boot/4.1.0/tutorial/first-application/index.html`：返回 `HTTP/2 200`。
- 线上教程页 HTML 包含 `spring-boot-zh-headerless`、左侧导航和正文标题 `开发你的第一个 Spring Boot 应用`。
- `rg '<header class="header"|id="topbar-nav"'` 检查线上教程页 HTML：无结果，默认顶部导航未残留。

## 去顶栏布局偏移回归验收

- `build/site/boot/4.1.0/upgrading.html` 包含新版 `spring-boot-zh-headerless` 样式。
- `build/site/boot/4.1.0/upgrading.html` 不包含 `<header class="header">` 或 `id="topbar-nav"`。
- 新版 headerless 样式已重置 `.toolbar`、`.nav` 和 `.toc.sidebar .toc-menu` 的顶部偏移。
- `curl -fsSI http://127.0.0.1:8080/boot/4.1.0/upgrading.html`：返回 `HTTP/1.1 200 OK`。
- Chrome headless 截图 `http://127.0.0.1:8080/boot/4.1.0/upgrading.html`：首屏无顶部大块空白，正文标题未被面包屑工具栏遮挡，右侧 Contents 位于正文顶部附近，左侧底部版本选择器可见。
- 已手动触发 Pages workflow：`https://github.com/LoreShare/spring-boot-zh-docs/actions/runs/27358407721`。
- `gh run view` 中部署 job 显示完成且结论为 `success`；GitHub 顶层 run 状态暂仍显示 `in_progress`，但线上页面已经更新。
- `curl -fsSI https://loreshare.github.io/spring-boot-zh-docs/boot/4.1.0/upgrading.html?ts=1781191845`：返回 `HTTP/2 200`。
- 线上 `upgrading.html` 不包含 `<header class="header">` 或 `id="topbar-nav"`。
- 线上 `upgrading.html` 包含新版 `spring-boot-zh-headerless` 偏移样式。
- Chrome headless 截图线上 `upgrading.html`：首屏无顶部大块空白，正文标题未被面包屑工具栏遮挡，右侧 Contents 位于正文顶部附近，左侧底部版本选择器可见。

## DeepSeek Token 用量

`reports/deepseek-usage.jsonl` 当前统计：

- 记录数：3,063
- 覆盖文件数：402
- prompt tokens：2,185,615
- completion tokens：1,555,120
- total tokens：3,740,735

说明：usage 日志只记录成功写入译文的请求，不包含少量失败请求和调试请求的实际消耗。

## 已知限制

- 当前站点不包含官方构建时生成的 Javadoc 和 Kotlin API 页面。
- Java API、Kotlin API、Maven Plugin API 和 Gradle Plugin API 保持为官方外部英文链接。
- 官方 Gradle 构建生成的 Antora 内容已导入中文站；其中代码、REST 请求/响应、日志和 schema 类输出片段按原文保留。
- `npm run audit:completeness` 当前仍会输出警告级别提示，主要用于后续人工排查 anchor 和低置信英文残留；警告不会阻塞构建。
- AI 翻译仍需要人工校对。

## 本地预览

构建后运行：

```bash
npm run serve
```

然后访问：

```text
http://localhost:8080
```
