# 验收记录

## 已完成

- 当前目录已初始化为 Git 仓库。
- 已固定上游 Spring Boot 版本为 `v4.1.0`。
- 已实现文档源同步脚本。
- 已实现 DeepSeek `deepseek-v4-flash` 翻译脚本。
- 已实现 AsciiDoc 校验脚本。
- 已实现 Antora 中文站构建配置。
- 已生成全量中文页面和 partial，顶层导航已切换到 `nav.adoc`。
- 已记录 DeepSeek 成功请求 token 用量到 `reports/deepseek-usage.jsonl`。

## 验收命令

```bash
npm test
npm run validate
npm run build
```

当前验收结果：

- `npm test`：通过，27 个测试。
- `npm run validate`：通过，未发现 AsciiDoc 结构、xref、术语或密钥问题。
- `npm run build`：通过，静态站生成到 `build/site`。

## DeepSeek Token 用量

`reports/deepseek-usage.jsonl` 当前统计：

- 记录数：1,517
- 覆盖文件数：151
- prompt tokens：1,058,213
- completion tokens：798,863
- total tokens：1,857,076

说明：usage 日志只记录成功写入译文的请求，不包含少量失败请求和调试请求的实际消耗。

## 已知限制

- 当前站点不包含官方构建时生成的 Javadoc 和 Kotlin API 页面。
- Java API、Kotlin API、Maven Plugin API 和 Gradle Plugin API 保持为官方外部英文链接。
- 官方构建流程生成的配置属性表、插件目标表和 Actuator REST API 请求/响应片段以中文占位 partial 补齐。
- AI 翻译仍需要人工校对。

## 本地预览

构建后运行：

```bash
npm run serve
```

然后访问：

```text
http://localhost:8080
```
