[[heapdump]]
= heapdump

`heapdump` 端点提供来自应用程序 JVM 的堆转储。


[[heapdump.retrieving]]
== 检索堆转储

要检索堆转储，请向 `/actuator/heapdump` 发送 `GET` 请求。
响应是二进制数据，可能很大。
其格式取决于应用程序运行的 JVM。
在 HotSpot JVM 上运行时，格式为 https://docs.oracle.com/javase/8/docs/technotes/samples/hprof.html[HPROF]，
在 OpenJ9 上则为 https://www.eclipse.org/openj9/docs/dump_heapdump/#portable-heap-dump-phd-format[PHD]。
通常，您应将响应保存到磁盘以供后续分析。
使用 curl 时，可以通过使用 `-O` 选项来实现，如下例所示：

include::partial$rest/actuator/heapdump/curl-request.adoc[]

前面的示例会在当前工作目录中生成一个名为 `heapdump` 的文件。
