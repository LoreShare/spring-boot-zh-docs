|===
|路径|类型|描述

|`+version+`
|`+String+`
|Java 版本（如果可用）。

|`+vendor+`
|`+Object+`
|供应商详细信息。

|`+vendor.name+`
|`+String+`
|供应商名称（如果可用）。

|`+vendor.version+`
|`+String+`
|供应商版本（如果可用）。

|`+runtime+`
|`+Object+`
|运行时详细信息。

|`+runtime.name+`
|`+String+`
|运行时名称（如果可用）。

|`+runtime.version+`
|`+String+`
|运行时版本（如果可用）。

|`+jvm+`
|`+Object+`
|JVM 详细信息。

|`+jvm.name+`
|`+String+`
|JVM 名称（如果可用）。

|`+jvm.vendor+`
|`+String+`
|JVM 供应商（如果可用）。

|`+jvm.version+`
|`+String+`
|JVM 版本（如果可用）。

|===
