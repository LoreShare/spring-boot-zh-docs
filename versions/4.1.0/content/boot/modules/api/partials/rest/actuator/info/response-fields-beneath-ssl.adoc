|===
|路径|类型|描述

|`+bundles+`
|`+Array+`
|SSL 捆绑信息。

|`+bundles[].name+`
|`+String+`
|SSL 捆绑的名称。

|`+bundles[].certificateChains+`
|`+Array+`
|捆绑中的证书链。

|`+bundles[].certificateChains[].alias+`
|`+String+`
|证书链的别名。

|`+bundles[].certificateChains[].certificates+`
|`+Array+`
|链中的证书。

|`+bundles[].certificateChains[].certificates[].subject+`
|`+String+`
|证书的主题。

|`+bundles[].certificateChains[].certificates[].version+`
|`+String+`
|证书的版本。

|`+bundles[].certificateChains[].certificates[].issuer+`
|`+String+`
|证书的签发者。

|`+bundles[].certificateChains[].certificates[].validityStarts+`
|`+String+`
|证书有效期起始日期。

|`+bundles[].certificateChains[].certificates[].serialNumber+`
|`+String+`
|证书的序列号。

|`+bundles[].certificateChains[].certificates[].validityEnds+`
|`+String+`
|证书有效期结束日期。

|`+bundles[].certificateChains[].certificates[].validity+`
|`+Object+`
|证书有效期信息。

|`+bundles[].certificateChains[].certificates[].validity.status+`
|`+String+`
|证书有效期状态。

|`+bundles[].certificateChains[].certificates[].signatureAlgorithmName+`
|`+String+`
|签名算法名称。

|`+bundles[].trustStoreCertificateChains+`
|`+Array+`
|信任库中的证书链。

|`+bundles[].trustStoreCertificateChains[].alias+`
|`+String+`
|证书链的别名。

|`+bundles[].trustStoreCertificateChains[].certificates+`
|`+Array+`
|链中的证书。

|`+bundles[].trustStoreCertificateChains[].certificates[].subject+`
|`+String+`
|证书的主题。

|`+bundles[].trustStoreCertificateChains[].certificates[].version+`
|`+String+`
|证书的版本。

|`+bundles[].trustStoreCertificateChains[].certificates[].issuer+`
|`+String+`
|证书的签发者。

|`+bundles[].trustStoreCertificateChains[].certificates[].validityStarts+`
|`+String+`
|证书有效期起始日期。

|`+bundles[].trustStoreCertificateChains[].certificates[].serialNumber+`
|`+String+`
|证书的序列号。

|`+bundles[].trustStoreCertificateChains[].certificates[].validityEnds+`
|`+String+`
|证书有效期结束日期。

|`+bundles[].trustStoreCertificateChains[].certificates[].validity+`
|`+Object+`
|证书有效期信息。

|`+bundles[].trustStoreCertificateChains[].certificates[].validity.status+`
|`+String+`
|证书有效期状态。

|`+bundles[].trustStoreCertificateChains[].certificates[].signatureAlgorithmName+`
|`+String+`
|签名算法名称。

|===
