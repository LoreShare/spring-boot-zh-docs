|===
|路径|类型|描述

|`+artifact+`
|`+String+`
|应用的构件 ID（如果有）。

|`+group+`
|`+String+`
|应用的组 ID（如果有）。

|`+name+`
|`+String+`
|应用的名称（如果有）。

|`+version+`
|`+String+`
|应用的版本（如果有）。

|`+time+`
|`+Varies+`
|应用构建时的时间戳（如果有）。

|===
